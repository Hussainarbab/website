# Copy this to .env and fill values for Facebook OAuth
# Example values for local testing
FACEBOOK_APP_ID=your_facebook_app_id_here
FACEBOOK_APP_SECRET=your_facebook_app_secret_here
# The public URL where your app is reachable. For local dev use http://localhost:5000
APP_URL=http://localhost:5000
# JWT secret used by the server (do NOT commit real secrets)
JWT_SECRET=change_this_in_production
# Email config (if used by auth)
EMAIL_SERVICE=gmail
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_password
# MongoDB (optional)
MONGODB_URI=
# Allow skip email verification in development
SKIP_EMAIL_VERIFICATION=true
